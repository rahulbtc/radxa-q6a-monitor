services:
  monitor:
    build: .
    container_name: radxa-q6a-monitor
    network_mode: host
    volumes:
      - /sys:/sys:ro
      - /proc:/proc:ro
      - /dev:/dev:ro
      - /var/run/docker.sock:/var/run/docker.sock:ro
    privileged: true
    restart: unless-stopped
